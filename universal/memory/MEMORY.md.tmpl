# Memory Index

Index for the auto-memory protocol. Entries are one line, under ~150 chars.
Memory files live in `memory/{user,feedback,project,reference}/`.

## User
*(no entries yet — write here when learning about the user's role, preferences, knowledge)*

## Feedback
*(no entries yet — validated approaches, corrections, conventions. Logs go to `logs/`, not here.)*

## Project
*(no entries yet — write here when a project decision should persist across sessions)*

## Reference
*(no entries yet — write here for external system pointers like dashboards, repos, tools)*
