---
description: AgentForge identity, stack defaults, and execution rules (always in context)
globs: "**/*"
alwaysApply: true
---

# Identity

{{IDENTITY_BLOCK}}

# Stack Defaults

{{STACK_BLOCK}}

# Execution Rules

{{EXECUTION_RULES}}

# Memory Protocol (pointer)

See `MEMORY.md` (generated index — keep in sync; logs live in `logs/`, not `memory/`) for the always-loaded memory index. Load bucket files (one fact per file, `[[wikilinks]]`) on demand:

{{MEMORY_PROTOCOL}}
