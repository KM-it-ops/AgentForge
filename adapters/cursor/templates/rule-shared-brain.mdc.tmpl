---
description: Shared brain (memory-mcp) — every agent reads on start, writes on completion
globs: "**/*"
alwaysApply: true
---

# Shared brain (memory-mcp)

**Cross-agent vault — not AgentForge.** Canonical brain: `C:\AI\brain`. MCP server: `memory`.

{{MCP_RITUAL_BLOCK}}

Do not skip `memory_write` when a meaningful task or goal completes.
