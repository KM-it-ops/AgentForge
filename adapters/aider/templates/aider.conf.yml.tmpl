# {{AF_BLOCK_BEGIN}}
# .aider.conf.yml — emitted by AgentForge (aider adapter).
# Aider reads this from your home dir, the git-repo root, and the cwd.
# Load the coding posture (CONVENTIONS.md) as read-only context on every run.
read: CONVENTIONS.md
{{MCP_BLOCK}}# {{AF_BLOCK_END}}
