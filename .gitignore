# Sandbox profiles for round-trip testing
.claude.test/
.codex.test/
.test-output/

# Build artifacts
node_modules/
dist/
*.tgz

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
*.log
*.tmp

# Local secrets (we have none, but defense in depth)
.env
.env.*
!.env.example

# Vercel CLI link metadata (local only; Git integration uses dashboard)
.vercel/

# Internal planning / agent handoff artifacts — not for public consumption
docs/goals/
*GOALBUDDY*
AGENTS.md
docs/GOALBUDDY-HANDOFF.md
