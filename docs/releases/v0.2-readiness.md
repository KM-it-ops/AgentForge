# AgentForge v0.2 Release Readiness Packet

Status: release candidate packet, not published.

Prepared on: 2026-06-10.

Latest evidence refresh: 2026-06-12.

## Baseline

| Item | Evidence | Status |
|---|---|---|
| Local branch | `main` | verified locally |
| Release-candidate commit | `f9a642f09e789cc029f533322cee7216ca981340` | pushed to `main` |
| Commit subject | `chore: prepare v0.2 release candidate` | verified locally and remotely |
| Fresh remote ref | GitHub API `repos/KM-it-ops/AgentForge/commits/main` | verified: `main` is `f9a642f09e789cc029f533322cee7216ca981340` |
| Fresh GitHub CI | `round-trip` Actions run `27439210914` | verified green for `f9a642f09e789cc029f533322cee7216ca981340` |
| Repository visibility | `gh repo view KM-it-ops/AgentForge` | private |
| Local-only work state at Scout | instruction notes and goal-board files were present but are not release artifacts | recorded |

The v0.2 release-candidate package commit is pushed and verified green. Before
tagging, creating a GitHub release, or publishing to npm, re-run the remote ref
and `round-trip` checks against the then-current `main` SHA so a documentation or
metadata follow-up cannot accidentally bypass CI proof.

## Versioning Decision

The v0.2 release candidate version is `0.2.0`.

The package metadata has been advanced from `0.1.1` to `0.2.0` so local pack and
install smoke tests exercise the intended public version before publish.
Publishing remains out of scope until the release checklist is complete and the
owner explicitly approves the exact npm publish operation.

## Package Metadata Audit

| Field | Current value | Status |
|---|---|---|
| `name` | `agentforge` | public registry returned 404; name appears unpublished, subject to owner publish-account approval |
| `version` | `0.2.0` | ready for local release-candidate smoke |
| `description` | configuration framework for agentic AI coding assistants | ready |
| `license` | `MIT` | ready |
| `author` | `Mahmoud Kurdi` | ready |
| `repository` | `https://github.com/KM-it-ops/AgentForge.git` | ready if repo visibility is intentional |
| `homepage` | `https://github.com/KM-it-ops/AgentForge#readme` | ready if repo visibility is intentional |
| `bugs` | GitHub issues URL | ready if issues are enabled |
| `bin` | `agentforge -> bin/agentforge.js` | ready |
| `main` | `bin/agentforge.js` | acceptable for CLI package |
| `engines` | `node >=18` | ready |
| `files` | `bin`, `CHANGELOG.md`, `docs/*.md` except internal handoff notes, `docs/demo`, `docs/releases`, `spec`, `scripts`, `universal`, `adapters`, `bootstrap`, `README.md` | verified by npm pack dry run |

Trust signals added for v0.2:

- `CHANGELOG.md`
- `LICENSE`
- this release readiness packet

## Package Contents And Install Smoke

Verified local release-candidate smoke:

```powershell
$env:PATH='C:\Program Files\nodejs;' + $env:PATH
$env:npm_config_cache=(Resolve-Path .\.test-output\npm-cache).Path
& 'C:\Program Files\nodejs\npm.cmd' run verify
& 'C:\Program Files\nodejs\npm.cmd' pack --dry-run --json
```

Reusable no-credential evidence collector:

```powershell
.\scripts\release-evidence.ps1
```

The collector writes `.test-output/release-evidence-v0.2.json`, runs local
doctor/pack/verify checks, records npm registry visibility for `agentforge`, and
attempts public GitHub ref/Actions checks. It does not login, publish, tag,
push, create GitHub releases, or pass credentials.

`npm run verify` ran both:

- `npm test`
- `npm run test:pack`

`npm run test:pack` proved:

- `npm pack` produces a tarball.
- the tarball installs into a clean throwaway npm project.
- the installed `agentforge` binstub reports `0.2.0`.
- `agentforge doctor --json` succeeds through the installed binstub.
- all four adapters emit through the installed CLI.

Package dry-run proof:

```text
agentforge@0.2.0
agentforge-0.2.0.tgz
entryCount: 100
includes CHANGELOG.md
includes docs/releases/v0.2-readiness.md
includes scripts/release-evidence.ps1
```

Environment note: the default managed shell still resolves `npm` through Volta
at `C:\Program Files\Volta\npm.exe`, and that shim fails with
`EPERM: operation not permitted, lstat 'C:\Users\alkur'`. Use the explicit
`C:\Program Files\nodejs\npm.cmd` path above for local release smoke in this
workspace.

## GitHub Release Checklist

Do not create the release until every required item is checked.

- [x] Fresh remote `main` commit equals the verified pushed baseline commit.
- [x] Latest GitHub Actions `round-trip` workflow is green for the verified
      pushed baseline on Ubuntu, Windows, and macOS for Node 20 and 22.
- [x] v0.2 release-candidate packet changes are committed and pushed.
- [x] Latest GitHub Actions `round-trip` workflow is green for the
      release-candidate commit on Ubuntu, Windows, and macOS for Node 20 and 22.
- [x] `npm run verify` passes locally or in a clean runner.
- [x] `npm pack --dry-run --json` confirms the intended package payload.
- [x] Installed tarball smoke confirms `agentforge --version` prints `0.2.0`.
- [x] README version badge and package version agree.
- [ ] `CHANGELOG.md` has final v0.2 notes and date.
- [x] Public npm registry check for `agentforge` returns 404, so no existing public package was found.
- [ ] Remaining blockers below are either closed or explicitly accepted by the
      owner.
- [ ] Owner approves tag creation.
- [ ] Owner approves GitHub release creation.
- [ ] Owner approves exact npm publish command and npm account/access scope.

Recommended release title:

```text
AgentForge v0.2.0
```

Recommended tag:

```text
v0.2.0
```

## Remaining Blockers

| Blocker | Required resolution |
|---|---|
| npm publish account/access scope not approved | Confirm the account and access scope that will publish the currently unpublished `agentforge` package. |
| GitHub repo is private | Confirm whether public npm release should point to this private GitHub repo as-is, or make the repo public before publish. |
| Release date not finalized | Fill the final date in `CHANGELOG.md` after the final green release audit. |
| Publish/tag/release approvals absent | Get explicit owner approval for each operation before acting. |

## Owner Evidence Needed

The local release packet is ready for an owner handoff, but final readiness still
requires evidence that cannot be collected from this managed shell without
credentials or a visible public GitHub surface.

On 2026-06-12, the owner approved carrying out the remaining evidence and
decision steps, including credentialed GitHub verification. The configured
credential paths are not currently usable:

- Sandboxed credential checks returned `401 Bad credentials`.
- Un-sandboxed GitHub CLI keyring auth works and has the required `repo` and
  `workflow` scopes.
- `gh repo view KM-it-ops/AgentForge` confirms the repository is private.
- `gh api repos/KM-it-ops/AgentForge/commits/main` confirmed remote `main` is
  `f9a642f09e789cc029f533322cee7216ca981340` during the latest evidence refresh.
- `gh run view 27439210914 --repo KM-it-ops/AgentForge` confirms the
  `round-trip` run for that SHA succeeded across:
  `ubuntu-latest` Node 20 and 22, `windows-latest` Node 20 and 22, and
  `macos-latest` Node 20 and 22.

The release-candidate package commit has been pushed and verified. Final release
approval still needs owner decisions for repository visibility, tag creation,
GitHub release creation, and npm publish scope.

Record these values before the final release-readiness audit:

| Evidence | Command or source | Required result | Recorded value |
|---|---|---|---|
| Remote `main` ref | `git ls-remote origin refs/heads/main` or GitHub API | equals intended release commit | verified for release-candidate commit `f9a642f09e789cc029f533322cee7216ca981340` |
| Latest `round-trip` workflow | GitHub Actions for the intended commit | green on Ubuntu, Windows, macOS for Node 20 and 22 | run `27439210914` green for release-candidate commit |
| npm name/package ownership | npm registry and owner approval | registry has no current public `agentforge` package; owner has publish authority for first publish | registry 404 verified; owner account/access approval pending |
| Repo visibility | GitHub repository settings or public page | visibility intentionally supports package metadata URLs | repository is private; owner must confirm private repo is acceptable for public npm metadata or approve making it public |
| Release date | owner release decision | final changelog date is known | _pending final green release audit_ |

Owner approval gates:

- [ ] Approve tag creation for `v0.2.0`.
- [ ] Approve GitHub release creation for `AgentForge v0.2.0`.
- [ ] Approve the exact npm publish command and account/access scope.
- [ ] Confirm no additional public-release blockers remain.

Checks attempted from this shell:

- GitHub REST API for commits and Actions returned `404 Not Found`.
- `git ls-remote` without credentials could not read a GitHub username.
- Public search did not surface `github.com/KM-it-ops/AgentForge`.
- Un-sandboxed `gh` with keyring credentials verified the private repo,
  remote `main`, and green baseline Actions run.
- Public npm registry returned `404 Not Found` for `agentforge`, so no existing public package was found.
- `scripts/release-evidence.ps1` is available to regenerate local proof and
  capture any future public GitHub visibility without using credentials.

## No-Publish Boundary

This packet does not publish to npm, create a GitHub release, create a git tag,
push commits, or use credentials. Those actions require explicit approval for
the exact target operation.
